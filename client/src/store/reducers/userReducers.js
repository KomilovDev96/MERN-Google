const defaultState = {
    curentUser: {},
    isAuth: false
}

export default function userReducers(state = defaultState, action) {
    switch (action.type) {
        default:
            return state
    }
}